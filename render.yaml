# render.yaml

services:
  - type: cron
    name: youtube-tracker-cron
    schedule: "30 20 * * *"  # Runs the script every day at 20:30 UTC
    command: "python app.py"
    env: python
    runtime: python3
    buildCommand: "pip install -r requirements.txt"
    startCommand: ""
    repo: https://github.com/PsaAI/youtube_tracker  # Replace with your GitHub repo URL
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: youtube-tracker-db
          property: connectionString
  - type: database
    name: youtube-tracker-db
    databaseName: youtube_tracker
    starterPlan: starter  # Use the free tier or upgrade as needed

